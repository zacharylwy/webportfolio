import "./productlist.css";
import Product from "../product/Product";
import { products } from "../../data";

const ProductList = () => {
  return (
    <div className="pl">
      <div className="pl-texts">
        <h1 className="pl-title">My projects - A work in progress</h1>
        <p className="pl-desc">
          Here are some of my works. It ain't much but I'm still working on it.
          Click on the image boxes to view them in greater detail.
        </p>
      </div>
      <div className="pl-list">
        {products.map((item) => (
          <Product key={item.id} img={item.img} link={item.link} />
        ))}
      </div>
    </div>
  );
};

export default ProductList;
